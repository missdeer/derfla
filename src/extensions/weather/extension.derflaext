id= "com.derfla.weather"
author= "missdeer"
name= "Weather Report"
if derfla.os.name == "windows" then
	executable= "weather.exe"
else
	executable= "weather"
end
description= "Query city weather report"
prefix={"w", "weather"}
waitIconPath= ""
waitTitle= "Querying..."
waitDescription= "Querying weather..."

