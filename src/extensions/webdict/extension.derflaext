id= "com.derfla.webdict"
author= "missdeer"
name= "Web Dictionary"
if derfla.os.name == "windows" then
	executable= "webdict.exe"
else
	executable= "webdict"
end
description= "Query web dictionary"
prefix={"dict", "yd", "youdao", "dl", "deepl"}
waitIconPath= ""
waitTitle= "Querying..."
waitDescription= "Querying on web dictionary..."

